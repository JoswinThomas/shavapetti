[build]
  command = "pip install -r backend/requirements.txt"

[deploy]
  startCommand = "gunicorn --bind 0.0.0.0:$PORT --workers 1 --timeout 300 backend.app:app"
  healthcheckPath = "/"
  healthcheckTimeout = 30
  restartPolicyType = "always"
